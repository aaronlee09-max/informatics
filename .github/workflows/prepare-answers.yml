name: Prepare answer notebooks

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  scan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Scan blanks and missing answer notebooks
        run: python3 tools/prepare_answers.py
